<strong>{{ price.stock.name }}</strong><br>
<strong>{{ price.stock.symbol }}</strong> | {{ price.stock.type }}<br>
{% if price.changeAbsolute > 0 %}📈{% else %}📉{% endif %}
<code>${{ price.stock.change }}</code> {{ price.stock.changePercent }}%<br>
<br>
{{ balance.totalAmount }}x <code>${{ price.stock.price }}</code> ({{ price.stock.honorPrice }} Ehre)<br>
<code>${{ balance.currentTotal }}</code> ({{ balance.currentHonorTotal }} Ehre)<br>
<br>
W/L <code>${{ balance.profit }}</code> ({{ balance.honorProfit }} Ehre)<br>